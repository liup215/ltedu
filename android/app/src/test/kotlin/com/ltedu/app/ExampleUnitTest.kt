package com.ltedu.app

import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Basic unit test to verify the test infrastructure is wired up correctly.
 * More focused tests live alongside each feature (ViewModel, Repository, etc.).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertTrue(2 + 2 == 4)
    }
}
